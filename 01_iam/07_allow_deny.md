# Allow deny

- When There are no Allow or Deny statements, Then it is implict deny. All access will be denide
- If we have ALlow statement, Then allow will override the impolicit deny
- If we have Deny statement, This is explicit deny. Then The Explicit deny Will override Allow statemnt


Explicit Deny > Allow > Implicit deny
