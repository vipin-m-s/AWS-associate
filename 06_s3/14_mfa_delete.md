# MFA delete
- second layer of protection against object deletion and disabling versioning
- Bucket vbersioning is required for mfa delete
- object version delteion
- object deletion
- suspending bucket versioning
- We can enable mfa delete only on
  - using root user
  - root user should have mfa enabled
  - can be done only using cli or sdk cannot do with console
  
