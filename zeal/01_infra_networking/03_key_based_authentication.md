# key based authentication 

Password based authentication is less secure since users use weak password, they write down password in plain text files. 

Private and public key will be generated 
public key will be stored in server
private key is used for authentication 

