# Athena federated query
- We can query data from other sources with the help of athena federated query
- It uses aws lambda connector for connecting to other sources such as on prem, elastic cache, rds etc
- query results are stored in s3
